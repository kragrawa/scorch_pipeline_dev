process.container = 'seurat_v5_image'
docker.enabled = true

process{
    executor = 'local'
    memory = '16 GB'
    cpus = 4

    withName: 'LabelTransferSingleSample' {
        memory = '128 GB'
        cpus = 4
    }
}